meta {
  name: Request Validate Acc
  type: http
  seq: 5
}

post {
  url: {{url}}/v1/validate-requests
  body: json
  auth: inherit
}

body:json {
  {
      "nik": "{{nik}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
