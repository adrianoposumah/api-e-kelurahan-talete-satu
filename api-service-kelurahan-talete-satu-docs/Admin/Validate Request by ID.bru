meta {
  name: Validate Request by ID
  type: http
  seq: 5
}

patch {
  url: {{url}}/v1/admin/validate-requests/:id
  body: json
  auth: inherit
}

params:path {
  id: 1
}

body:json {
  {
      // approved or rejected
      "status": "approved"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
